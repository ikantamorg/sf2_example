{% extends 'CoreBundle:Emails:mail.base.html.twig' %}

{% block yeld %}
    Hello {{ expert_user.fullName }}.
    <br>
    <br>
    The candidate "{{  candidate.fullName }}" changed session's start date with you.
    <br>
    <br>
    Old start date was: {{ oldAppointment.startDate|date(date_time_formats.date_time_with) }} -  {{ oldAppointment.endDate|date(date_time_formats.date_time_with) }} {{ oldAppointment.startDate|date(date_time_formats.timezone) }}
    <br>
    Changed session start date is: {{ appointment.startDate|date(date_time_formats.date_time_with) }} - {{ appointment.endDate|date(date_time_formats.date_time_with) }} {{ appointment.startDate|date(date_time_formats.timezone) }}
    <br>
    <br>
    If you are satisfied - approve a request.
{% endblock %}
