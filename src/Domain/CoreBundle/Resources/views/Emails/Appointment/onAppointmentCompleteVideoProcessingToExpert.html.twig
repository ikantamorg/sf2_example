{% extends 'CoreBundle:Emails:mail.base.html.twig' %}

{% block yeld %}
    Hello {{  expert_user.fullName }}.
    <br>
    <br>
    The session's video with {{ candidate.fullName }} ({{ appointment.startDate|date(date_time_formats.date_time_with_timezone) }} -  {{ appointment.endDate|date(date_time_formats.date_time_with_timezone) }}) was successfully Processed.
    <br>
    <br>
    <a href="{{ url('expert_after_appointment_index', {appointmentId: appointment.id}) }}">View</a> it or <a href="{{ download_url }}">download</a>.
{% endblock %}
