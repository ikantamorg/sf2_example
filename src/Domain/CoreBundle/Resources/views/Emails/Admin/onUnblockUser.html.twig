{% extends 'CoreBundle:Emails:mail.base.html.twig' %}

{% block yeld %}
    Hello {{ user.fullName }},
    <br/>
    Admin unlocked your account.
    {% if(reason|length > 0) %}
        <br>
        <br>
        Reason:
        <br>
        {{ reason }}
    {% endif %}
{% endblock %}